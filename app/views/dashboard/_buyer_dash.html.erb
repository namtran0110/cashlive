<div id="buyer_dash" role="tabpanel" class="tab-pane fade show active">

  <h2>Dashboard for user: <strong><%= @user.username %></strong></h2>

  <p>This is like the homepage for a logged in user. It will display popular current streams, a list of subscribed channels, and maybe other things</p>
  <p>It will also have a link for "seller dashboard", which could be a tab or a separate page</p>

<h2>Stores streaming now:</h2>
<% if @streaming_stores.count > 0 %>
  <ul>
    <% @streaming_stores.each do |store| %>
      <li>
        <p><%= store.name %> <%= link_to "view store", store_path(store), class: 'btn btn-secondary btn-small' %></p>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>No stores are currently streaming</p>
<% end %>

  <h2>Stores I subscribe to:</h2>
  <ul>
    <li>
      <p>{{Tho to configure this list}}</p>
    </li>
  </ul>

  <h2>Other Popular stores:</h2>
  <% if @stores.count > 0 %>
    <ul>
      <% @stores.each do |store| %>
        <li>
          <p><%= store.name %> <%= link_to "view store", store_path(store), class: 'btn btn-secondary btn-small' %></p>
        </li>
      <% end %>
    </ul>
  <% end %>
</div>
